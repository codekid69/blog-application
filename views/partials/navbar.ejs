<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">

    <!-- Brand -->
    <a class="navbar-brand" href="/">Bloggo</a>

    <!-- Hamburger Content (Move to the right) -->
    <div class="d-flex align-items-center ms-auto">
      <% if (user) { %>
        <!-- Profile Image + Name (visible on both mobile and desktop) -->
        <div class="d-flex align-items-center gap-2 text-white me-3">
          <img src="<%= user.profileImageUrl||user.pic || '/images/useravatar.png' %>" alt="profile" class="rounded-circle" width="35" height="35" style="object-fit: cover;">
          <span><%= user.name %></span>
        </div>
      <% } else { %>
        <div class="d-flex align-items-center gap-2 text-white me-3">
          <img src="<%='/images/useravatar.png' %>" alt="guest-profile" class="rounded-circle" width="35" height="35" style="object-fit: cover;">
          <span>Hi, Guest</span>
        </div>
      <% } %>

      <!-- Toggler Button (Hamburger) -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent"
        aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
    </div>

    <!-- Hamburger Content -->
    <div class="collapse navbar-collapse" id="navbarContent">
      <ul class="navbar-nav ms-auto mb-2 mb-lg-0 flex-column flex-lg-row align-items-lg-center gap-lg-3">

        <% if (user) { %>
          <!-- My Blogs Link -->
          <li class="nav-item mb-2 mb-lg-0">
            <a class="nav-link text-white" href="/blog/myblogs">My Blogs</a>
          </li>

          <!-- Add Blog -->
          <li class="nav-item mb-2 mb-lg-0">
            <a class="nav-link text-white" href="/blog">Add Blog</a>
          </li>

          <!-- Settings -->
          <li class="nav-item mb-2 mb-lg-0">
            <a class="nav-link text-white" href="/user/settings">Settings</a>
          </li>

          <!-- Logout -->
          <li class="nav-item mb-2 mb-lg-0">
            <form action="/user/logout" method="POST" class="d-inline">
              <button class="btn btn-outline-light" type="submit">Logout</button>
            </form>
          </li>
        <% } else { %>
          <!-- Login Button -->
          <li class="nav-item mb-2 mb-lg-0">
            <a class="btn btn-outline-light" href="/user/signin">Login</a>
          </li>
        <% } %>

      </ul>
    </div>
  </div>
</nav>
